# Locked Scope — Review Round 2 (2026-08-18)

Supersedes Round 1 (2026-08-15) for notebook flow and structure. Round 1's points 1-3 (GLM/XGBoost framing, costing analysis, fraud detection) are unaffected and remain as built in notebook 02. Round 1 point 4 (multi-state IP realism) is fully superseded — resolved and built out in notebook 04 across an extended working session; see notebook 04's own locked design (semi-Markov duration model, age × occupation base rate, deferred-period restatement, Bühlmann-credibility experience loading).

## Notebook flow (6 teaching notebooks + 1 supporting data notebook)

1. **`01_genai_basics.ipynb`** — unchanged. Raw API calls, CCCE prompting framework, hallucination demo, structured output.
2. **`02_models_as_tools.ipynb`** — unchanged. GLM (production) vs XGBoost (diagnostic/challenger) vs SHAP framing, per Round 1 point 1. No costing-analysis module (Round 1 point 2 shelved, not built into this notebook).
3. **`03_first_agent.ipynb`** — unchanged in substance. Simple single-tool agent looking up Critical Illness incidence rates (Cardiac 2.4%, Cancer 3.1%, Stroke 1.5%) — the "raw mechanic → hello agent → break it" arc. Auth cell brought in line with the Colab Secrets pattern used elsewhere (was using a local `.env`/`dotenv` pattern inconsistent with the rest of the series).
4. **`04_ip_pricing_agent.ipynb`** — unchanged (per explicit confirmation), i.e. no Break It / Guardrail Fix sections in this cut. Full semi-Markov IP pricing agent: age × occupation incidence (Tool 1), deferred-period severity/frequency restated from one dataset (Tool 2), Bühlmann-credibility episode loading (Tool 3), 4 guardrails, Agno agent, and the full policyholder + actuary visual dashboards (Section 11).
5. **`05_pricing_team.ipynb`** (rebuilt from `05_actuarial_analyst_agent.ipynb`) — **no data generation.** Loads pre-built artifacts from the data notebook (below). Two specialist agents only:
   - **IP agent** — the real Tool 1/2/3 + guardrails from notebook 04, reused verbatim. Answers with a premium quote, plain-English explanation, and 3 charts (premium waterfall, incidence heatmap, deferred-period frequency/severity trade-off) — same chart set ported to the Streamlit app.
   - **PMI agent** — a new, simpler frequency × severity = pure premium calculator, calibrated to the established canonical PMI figures (portfolio frequency 6%, severity ₹85,000, pure premium ₹5,100), rated by age band, sum-insured band, and NCB tier. **Explicitly excludes expense and profit loading** — pure risk premium only, a deliberate simplification distinct from the fuller PMI Pricing Logic Explainer's worked example.
   - **Orchestrator**: `agno.team.Team` in `TeamMode.route` — the leader inspects the query and routes to whichever agent fits, or answers directly using its own RAG tool for policy/assumption questions.
   - **RAG**: lightweight TF-IDF search (same technique as the prior notebook 05's mini-RAG) over two new markdown policy documents — `ip_policy_assumptions.md` and `pmi_policy_assumptions.md` — capturing every modelling assumption behind each product (deferred period options, waiver of premium, 0% expense/profit loading, rating factors, etc.) so the team can answer "what does this coverage assume" questions from a citable source rather than the LLM's own knowledge.
   - **Dropped from the prior notebook 05, not carried forward**: the §3b IP life-projection matrix tool (superseded by notebook 04's real model) and the Reviewer/peer-review agent pattern (out of scope for this notebook; not relocated elsewhere).
6. **`06_advanced_topics_and_case_study.ipynb`** (rebuilt from `06_case_study_starter.ipynb`) — new front section: brief theory + small examples on advanced agentic AI patterns (multi-agent orchestration modes, RAG grounding, where guardrails scale beyond single-tool checks). Existing case-study-starter material (spec template, datasets, reusable scaffolds, ten-question checklist) kept as the second half, unchanged in substance.

**`data_notebook.ipynb`** (new, supporting — not part of the numbered 6) — generates and pickles the IP artifacts (population, spells, Tool 1/2/3 tables — same generation code as notebook 04) and the new PMI artifacts (a modest synthetic policy-level dataset plus the calibrated frequency/severity/NCB tables), for notebook 05 to load. Run once per cohort/session; outputs are meant to be placed in the shared repo (or the same Colab/local working directory) before notebook 05 is used, exactly mirroring the `build_data.py` → `pricing_artifacts.pkl` pattern already used in the Streamlit app.

## Status
All open points from Round 1 are now resolved (point 4 via notebook 04's build) or superseded by this round. Ready for build.
